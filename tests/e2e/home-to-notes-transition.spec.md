# Home → Notes transition E2E

Run with:

```
python3 tests/e2e/home_to_notes_transition.py
```

Verifies Home never appears in the DOM while Notes is mounting (i.e. no
overlap during route transitions).
